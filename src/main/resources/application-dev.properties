# ─── Perfil DEV ──────────────────────────────────────────────────────────────
# Ativo automaticamente (padrao em application.properties)
# Override com: java -jar app.jar --spring.profiles.active=dev

spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=false

